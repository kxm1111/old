我来帮你创建这个Maven项目。先看一下现有的项目结构。





了解了，现在我来创建这个项目。关键是将servlet-api依赖设置为`provided`，这样打包时不会包含servlet依赖。
```bash
mkdir -p "d:\qoder\Project\打jar包不包含依赖\src\main\java\com\example"
```



代码创建完成，现在执行Maven打包：
```bash
cd "d:\qoder\Project\打jar包不包含依赖" ; mvn clean package
```
打包成功！验证一下jar包：
```bash
ls -lh "d:\qoder\Project\打jar包不包含依赖\target\hello-jar.jar"
```


```bash
Get-ChildItem "d:\qoder\Project\打jar包不包含依赖\target" | Select-Object Name, Length
```


```bash
Get-ChildItem "d:\qoder\Project\打jar包包含依赖\target" | Select-Object Name, Length
```

验证一下jar包中不包含servlet-api：
```bash
jar tf "d:\qoder\Project\打jar包不包含依赖\target\hello-jar.jar" | findstr "servlet"
```

项目创建完成！让我总结一下：

## 完成内容

**项目结构** (`打jar包不包含依赖`):
- [pom.xml](file:///d:\qoder\Project\打jar包不包含依赖\pom.xml) - 配置fastjson、servlet-api（provided）、tomcat-embed
- [XmServlet.java](file:///d:\qoder\Project\打jar包不包含依赖\src\main\java\com\example\XmServlet.java) - 使用fastjson生成`{name:"小美"}`JSON
- [Main.java](file:///d:\qoder\Project\打jar包不包含依赖\src\main\java\com\example\Main.java) - 启动嵌入式Tomcat，注册`/xm`路径

**关键配置**：
- servlet-api设置`<scope>provided</scope>`，这样不会打包显式的servlet依赖

**生成的jar包**：
- 路径：`d:\qoder\Project\打jar包不包含依赖\target\hello-jar.jar`
- 大小：约7.51 MB

**运行方式**：
```bash
java -jar d:\qoder\Project\打jar包不包含依赖\target\hello-jar.jar
```
然后访问 `http://localhost:8080/xm` 会返回 `{"name":"小美"}`

> 注意：虽然显式的servlet-api依赖被设置为provided不会打包，但Tomcat嵌入式库本身包含了Servlet实现类，所以功能正常。这是嵌入式Tomcat的正常行为。